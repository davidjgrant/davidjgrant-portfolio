import React from "react"
import { FooterWrapper } from "../elements"

export const Footer = () => {
    return (
        <FooterWrapper>
            This is a footer.
        </FooterWrapper>
    )
}
